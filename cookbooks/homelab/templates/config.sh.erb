#!/bin/bash
#
# cr.imson.co
#
# homelab configuration
# @author Damian Bushong <katana@odios.us>
#

# set sane shell env options
set -o errexit -o pipefail -o noclobber -o nounset

<% @configs.each do |key, value| -%>
<%= @config_name.upcase + '_' + key.upcase + '=' + value.to_s %>
<% end -%>
